---
phase: 13-xrpbtc-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/symbol_registry.py
  - backend/app/domain/models.py
  - backend/app/domain/lots.py
  - backend/app/db/models.py
  - backend/app/services/lot_service.py
  - backend/alembic/versions/xxxx_readd_quote_to_eur_rate.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03

must_haves:
  truths:
    - "XRPBTC exists in KNOWN_PAIRS and parse_symbol('XRPBTC') returns correct TradingPair"
    - "is_eur_quoted('XRPBTC') returns False, is_eur_quoted('BTCEUR') returns True"
    - "is_pairing_enabled('XRPBTC') returns False"
    - "is_order_creation_enabled('XRPBTC') returns False"
    - "TradeLotDB has quote_to_eur_rate column after migration"
    - "Domain create_trade_lot_from_buy_fill for BTC-quoted fill sets cost_eur to None (awaiting service-layer rate)"
    - "Lot API response includes quote_to_eur_rate field"
  artifacts:
    - path: "backend/app/symbol_registry.py"
      provides: "XRPBTC in KNOWN_PAIRS, helper functions is_eur_quoted, is_pairing_enabled, is_order_creation_enabled"
      contains: "XRPBTC"
    - path: "backend/app/domain/lots.py"
      provides: "Conditional EUR cost calculation (EUR-quoted vs BTC-quoted)"
      contains: "quote_asset"
    - path: "backend/app/db/models.py"
      provides: "quote_to_eur_rate column on TradeLotDB"
      contains: "quote_to_eur_rate"
    - path: "backend/alembic/versions/xxxx_readd_quote_to_eur_rate.py"
      provides: "Forward migration re-adding quote_to_eur_rate to trade_lots"
      contains: "quote_to_eur_rate"
  key_links:
    - from: "backend/app/domain/lots.py"
      to: "backend/app/symbol_registry.py"
      via: "get_quote_asset(symbol) to branch EUR vs BTC cost calculation"
      pattern: "get_quote_asset"
    - from: "backend/app/services/lot_service.py"
      to: "backend/app/db/models.py"
      via: "quote_to_eur_rate field in _lot_db_to_dict serialization"
      pattern: "quote_to_eur_rate"
---

<objective>
Add XRPBTC to Symbol Registry, create Alembic migration for quote_to_eur_rate column, update domain logic for BTC-quoted EUR cost branching, and extend lot API response.

Purpose: Lays the backend foundation (registry, schema, domain, serialization) that all subsequent plans depend on.
Output: XRPBTC in registry, migration file, domain branching, lot dict with quote_to_eur_rate.
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-xrpbtc-infrastructure/13-CONTEXT.md
@.planning/phases/13-xrpbtc-infrastructure/13-RESEARCH.md
@backend/app/symbol_registry.py
@backend/app/domain/lots.py
@backend/app/domain/models.py
@backend/app/db/models.py
@backend/app/services/lot_service.py
@backend/alembic/versions/094dac6f695a_remove_xrpbtc_cross_pair_columns.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Symbol Registry + Migration + ORM</name>
  <files>
    backend/app/symbol_registry.py
    backend/app/db/models.py
    backend/alembic/versions/xxxx_readd_quote_to_eur_rate.py
  </files>
  <action>
1. **symbol_registry.py** -- Add XRPBTC to KNOWN_PAIRS:
   ```python
   "XRPBTC": TradingPair("XRPBTC", "XRP", "BTC", 2, 8, "XRP/BTC"),
   ```
   Add three new helper functions:
   - `is_eur_quoted(symbol: str) -> bool` -- returns `parse_symbol(symbol).quote_asset == "EUR"`
   - `is_pairing_enabled(symbol: str) -> bool` -- returns `is_eur_quoted(symbol)` (pairing only for EUR-quoted pairs per user decision)
   - `is_order_creation_enabled(symbol: str) -> bool` -- returns `is_eur_quoted(symbol)` (manual sell orders only for EUR-quoted pairs per user decision)
   Note: XRPBTC price_precision=8 (BTC has 8 decimal places as quote asset).

2. **db/models.py** -- Add `quote_to_eur_rate` column to TradeLotDB class (after cost_eur):
   ```python
   quote_to_eur_rate = Column(Numeric(precision=20, scale=10), nullable=True)
   ```

3. **Alembic migration** -- Create new migration via `alembic revision -m "readd quote_to_eur_rate for v3.0"`. The migration:
   - Uses `batch_alter_table('trade_lots')` (SQLite requirement) to add `quote_to_eur_rate` as `Numeric(precision=20, scale=10), nullable=True`
   - Backfills EUR-quoted lots with `quote_to_eur_rate = 1.0` via SQL UPDATE (same pattern as research code example)
   - Does NOT re-add pairing columns (base_asset, routing_decision_json, cost_eur on pairing_items, lot_symbol, realized_pnl_eur) -- pairing is disabled for XRPBTC per user decision
   - Run `alembic upgrade head` to apply
  </action>
  <verify>
    Run: `cd /Users/ikoerber/AIProjects/cashmgnt/backend && source venv/bin/activate && python -c "from app.symbol_registry import parse_symbol, is_eur_quoted, is_pairing_enabled, is_order_creation_enabled; p = parse_symbol('XRPBTC'); assert p.quote_asset == 'BTC'; assert not is_eur_quoted('XRPBTC'); assert is_eur_quoted('BTCEUR'); assert not is_pairing_enabled('XRPBTC'); assert not is_order_creation_enabled('XRPBTC'); print('OK')"` and `alembic upgrade head` succeeds.
  </verify>
  <done>XRPBTC in KNOWN_PAIRS with BTC quote asset. TradeLotDB has quote_to_eur_rate column. Migration applied. EUR-quoted lots backfilled with rate=1.0. Helper functions is_eur_quoted, is_pairing_enabled, is_order_creation_enabled all work correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Domain Logic + Lot Serialization</name>
  <files>
    backend/app/domain/lots.py
    backend/app/domain/models.py
    backend/app/services/lot_service.py
  </files>
  <action>
1. **domain/models.py** -- Add `quote_to_eur_rate: Optional[Decimal] = None` field to the `TradeLot` dataclass (after cost_eur). Also add a `break_even_eur` property:
   ```python
   @property
   def break_even_eur(self) -> Optional[Decimal]:
       """Break-even in EUR (for BTC-quoted pairs, uses cost_eur)."""
       if self.cost_eur is None or self.qty_base_initial == 0:
           return None
       return self.cost_eur / self.qty_base_initial
   ```

2. **domain/lots.py** -- Modify `create_trade_lot_from_buy_fill()` lines 97-98. Replace the hardcoded `computed_cost_eur = cost_quote` with conditional logic:
   ```python
   # EUR Cost Basis: conditional on quote asset
   if quote_asset == "EUR":
       computed_cost_eur = cost_quote
   else:
       # BTC-quoted: cost_eur will be set by service layer after fetching historical BTC/EUR rate
       computed_cost_eur = None
   ```
   This is the critical change -- BTC-quoted pairs cannot compute EUR cost in the domain layer because it requires an external API call (Binance Klines). The service layer will set cost_eur after fetching the rate.

3. **services/lot_service.py** -- In `_lot_db_to_dict()`, add `quote_to_eur_rate` to the returned dict (after break_even_eur):
   ```python
   "quote_to_eur_rate": str(lot_db.quote_to_eur_rate) if lot_db.quote_to_eur_rate else None,
   ```
   Also ensure the existing `break_even_eur` computation handles quote_to_eur_rate being None gracefully (it already does via `lot_db.cost_eur is not None` check on line 947).

4. **services/lot_service.py** -- In `create_lot_from_buy_fill()`, after creating the TradeLotDB instance, add logic to persist `quote_to_eur_rate`:
   - If `lot_domain.cost_eur is not None` (EUR-quoted): set `lot_db.quote_to_eur_rate = Decimal("1")`
   - If `lot_domain.cost_eur is None` (BTC-quoted): leave as None (will be set by sync_service after historical rate fetch)
  </action>
  <verify>
    Run: `cd /Users/ikoerber/AIProjects/cashmgnt/backend && source venv/bin/activate && pytest tests/test_lots_fifo.py tests/test_portfolio_breakeven.py -x -q` -- all existing tests pass (no regression). Manually verify `python -c "from app.domain.lots import create_trade_lot_from_buy_fill; from app.domain.models import LedgerEvent, EventType, TradeSide; from decimal import Decimal; from datetime import datetime; e = LedgerEvent(id='t1', type=EventType.TRADE_FILL, side=TradeSide.BUY, amount=Decimal('100'), price=Decimal('0.00002345'), timestamp=datetime.now(), user_id='u1', symbol='XRPBTC'); lot = create_trade_lot_from_buy_fill(e); assert lot.cost_eur is None; print('BTC-quoted: cost_eur is None - OK')"`.
  </verify>
  <done>Domain layer branches on quote_asset for EUR cost. BTC-quoted lots get cost_eur=None from domain (service layer fills it). TradeLot dataclass has quote_to_eur_rate field and break_even_eur property. Lot API response includes quote_to_eur_rate. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `parse_symbol('XRPBTC')` returns TradingPair with quote_asset='BTC'
- `is_eur_quoted('XRPBTC')` returns False; `is_eur_quoted('BTCEUR')` returns True
- `is_pairing_enabled('XRPBTC')` returns False
- `is_order_creation_enabled('XRPBTC')` returns False
- `alembic upgrade head` succeeds, `quote_to_eur_rate` column exists on trade_lots
- Existing EUR-quoted lots have `quote_to_eur_rate = 1.0`
- Domain `create_trade_lot_from_buy_fill` for XRPBTC returns lot with `cost_eur = None`
- `pytest tests/ -x -q` -- all existing tests pass (no regression)
</verification>

<success_criteria>
XRPBTC is in the backend symbol registry with correct metadata. The database has the quote_to_eur_rate column restored. Domain logic correctly branches EUR vs BTC-quoted cost calculation. Lot API response includes the new field. Zero test regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-xrpbtc-infrastructure/13-01-SUMMARY.md`
</output>
