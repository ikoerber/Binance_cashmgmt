---
phase: 10-css-variable-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
autonomous: true
requirements:
  - DARK-01
  - DARK-02

must_haves:
  truths:
    - "All CSS color tokens are defined in :root in index.css with semantic names"
    - "A [data-theme='dark'] block exists in index.css with matching dark values for every token"
    - "Existing 22 CSS variables remain unchanged (names and values identical)"
    - "The app looks identical in the browser after this plan (no visual change)"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Complete CSS Custom Property system with light and dark palettes"
      contains: "[data-theme=\"dark\"]"
  key_links:
    - from: "frontend/src/index.css :root"
      to: "frontend/src/index.css [data-theme='dark']"
      via: "Matching token names with different values"
      pattern: "--color-(bg|text|border|accent|profit|loss|success|error|warning|info)"
---

<objective>
Define the complete CSS Custom Property system in index.css -- all light-mode tokens in :root and the full dark-mode palette in [data-theme="dark"].

Purpose: This is the foundation that Plans 02 and 03 will reference when replacing hardcoded hex values. Without these variable definitions, component CSS files cannot use var() references. The dark palette (DARK-02) must also be defined now so Phase 11 can activate it without re-touching index.css.

Output: Expanded index.css with ~80 tokens in :root and matching dark-mode overrides in [data-theme="dark"].
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-css-variable-foundation/10-RESEARCH.md

@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand :root with complete light-mode token system</name>
  <files>frontend/src/index.css</files>
  <action>
Expand the existing `:root` block in `index.css` to include ALL color tokens needed by the 12 CSS files + JSX inline styles. Keep the existing 22 variables EXACTLY as they are (same names, same values). Add ~50-60 new variables.

Use the token naming convention from research: `--color-{category}-{variant}`.

New tokens to add (organized by category):

**Text (extend existing):**
- `--color-text-dark: #334155` (slightly darker than primary, used in headings/labels)
- `--color-text-body: #475569` (between primary and secondary, used in table cells)
- `--color-text-inverse: #ffffff` (white text on colored backgrounds)

**Surface/Background (extend existing):**
- `--color-bg-page` already exists (#f5f7fa) -- also move the hardcoded `background-color: #f5f7fa` on `:root` to use `var(--color-bg-page)`
- `--color-bg-hover: #f1f5f9` (row/card hover states)
- `--color-bg-selected: #eef2ff` (selected rows, active states)
- `--color-bg-footer: #1e293b` (footer and subnav, dark elements in light theme)
- `--color-bg-input: #ffffff` (form inputs)
- `--color-bg-disabled: #f9fafb` (disabled input backgrounds)

**Borders (extend existing):**
- `--color-border-input: #d1d5db` (form input borders)
- `--color-border-focus: #6366f1` (focused input borders -- indigo accent)

**Warning feedback (new):**
- `--color-warning-bg: #fef3c7`
- `--color-warning-text: #92400e`
- `--color-warning-border: #fcd34d`

**Info feedback (new):**
- `--color-info-bg: #eff6ff`
- `--color-info-text: #1e40af`
- `--color-info-border: #bfdbfe`

**Badge/accent variants (new):**
- `--color-badge-purple-bg: #f3e8ff`
- `--color-badge-purple-text: #6b21a8`
- `--color-badge-blue-bg: #dbeafe`
- `--color-badge-blue-text: #1e40af`
- `--color-badge-amber-bg: #fef3c7`
- `--color-badge-amber-text: #92400e`
- `--color-badge-emerald-bg: #d1fae5`
- `--color-badge-emerald-text: #065f46`
- `--color-badge-slate-bg: #f1f5f9`
- `--color-badge-slate-text: #475569`
- `--color-accent-indigo: #818cf8` (indigo accent for pairing highlights)
- `--color-accent-indigo-light: #eef2ff` (indigo accent background)

**Table-specific (new):**
- `--color-table-header-bg: #f8fafc`
- `--color-table-row-even: #fafbfd`
- `--color-table-row-hover: #f1f5f9`

**Gradient endpoints (new):**
- `--color-gradient-section-start: #f8fafc`
- `--color-gradient-section-end: #f1f5f9`
- `--color-gradient-success-start: #ecfdf5`
- `--color-gradient-success-end: #d1fae5`
- `--color-gradient-navbar-start: #667eea` (same as accent-navbar-start, for gradient usage)
- `--color-gradient-navbar-end: #764ba2` (same as accent-navbar-end, for gradient usage)

**Status/state colors (new):**
- `--color-status-open: #16a34a`
- `--color-status-partial: #d97706`
- `--color-status-closed: #64748b`
- `--color-status-cancelled: #dc2626`
- `--color-status-draft: #6366f1`
- `--color-status-locked: #d97706`
- `--color-status-executed: #16a34a`

**Misc (new):**
- `--color-link: #646cff`
- `--color-link-hover: #535bf2`
- `--color-button-bg: #f9f9f9`
- `--color-focus-ring: #646cff`
- `--color-scrollbar-track: #f1f5f9`
- `--color-scrollbar-thumb: #cbd5e1`
- `--color-bg-warning-row: #fffbeb` (LotsTable warning row)
- `--color-profit-bright: #22c55e` (brighter profit for dark-compatible usage)

**Shadow variables (extend existing):**
- Keep existing `--shadow-card`, `--shadow-hover`, `--shadow-sm` unchanged

Also convert the two hardcoded colors in `:root` itself:
- `color: #213547` -> `color: var(--color-text-primary)` (close enough to #1e293b)
- `background-color: #f5f7fa` -> `background-color: var(--color-bg-page)`

Also convert global selectors below `:root`:
- `a { color: #646cff }` -> `a { color: var(--color-link) }`
- `a:hover { color: #535bf2 }` -> `a:hover { color: var(--color-link-hover) }`
- `button { background-color: #f9f9f9 }` -> `button { background-color: var(--color-button-bg) }`
- `button:hover { border-color: #646cff }` -> `button:hover { border-color: var(--color-focus-ring) }`

IMPORTANT: Do NOT remove or rename any of the existing 22 variables. Only ADD new ones and convert the few hardcoded values in index.css itself.

The final token count should be approximately 72-82 variables in `:root`. Organize them with clear comment section headers for each category.

When examining the actual CSS files during Plan 02/03, some additional tokens may be discovered. This is acceptable -- the executor for those plans can add missing tokens to index.css. But this task should cover all tokens identified in the research inventory.
  </action>
  <verify>
Run `grep -c 'var(--' frontend/src/index.css` to confirm variable usage in index.css global selectors.
Run `grep -c '\-\-color-' frontend/src/index.css` to confirm ~80+ token definitions exist.
Run `grep '#[0-9a-fA-F]' frontend/src/index.css` and verify hex values appear ONLY inside `:root {}` variable definitions and `[data-theme="dark"] {}` block (not in body/a/button selectors).
Run `npm run build` from frontend/ to verify no CSS syntax errors.
  </verify>
  <done>
:root block contains ~72-82 CSS custom property definitions organized by category. Global selectors (a, button, body) use var() references instead of hardcoded hex. No CSS syntax errors. All existing 22 variables unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define [data-theme="dark"] palette block</name>
  <files>frontend/src/index.css</files>
  <action>
Add a `[data-theme="dark"]` block AFTER the `:root` block in `index.css` that overrides every color token with dark-mode values. This block is DEFINED but NOT ACTIVE -- no JavaScript toggles it yet (that is Phase 11).

Use the dark palette from the research (10-RESEARCH.md Example 5), expanding it to cover ALL tokens defined in Task 1.

Dark palette values (from research + extended for new tokens):

**Surface Hierarchy:**
- `--color-bg-page: #0f1117`
- `--color-bg-card: #161822`
- `--color-bg-subtle: #1e2030`
- `--color-bg-muted: #252838`
- `--color-bg-hover: #252838`
- `--color-bg-selected: #1e2448`
- `--color-bg-footer: #0f1117` (same as page in dark)
- `--color-bg-input: #1e2030`
- `--color-bg-disabled: #161822`
- `--color-bg-warning-row: #1c1408`

**Text Hierarchy:**
- `--color-text-primary: #e2e8f0`
- `--color-text-secondary: #94a3b8`
- `--color-text-muted: #64748b`
- `--color-text-heading: #f1f5f9`
- `--color-text-dark: #cbd5e1`
- `--color-text-body: #94a3b8`
- `--color-text-inverse: #0f1117`

**Borders:**
- `--color-border: #2e3348`
- `--color-border-light: #252838`
- `--color-border-input: #3e4458`
- `--color-border-focus: #818cf8`

**Semantic:**
- `--color-profit: #22c55e`
- `--color-loss: #ef4444`
- `--color-profit-bright: #4ade80`

**Feedback States:**
- Success: `--color-success-bg: #052e16`, `--color-success-text: #22c55e`, `--color-success-border: #166534`
- Error: `--color-error-bg: #2a0f0f`, `--color-error-text: #ef4444`, `--color-error-border: #991b1b`
- Warning: `--color-warning-bg: #1c1408`, `--color-warning-text: #fbbf24`, `--color-warning-border: #92400e`
- Info: `--color-info-bg: #0c1929`, `--color-info-text: #38bdf8`, `--color-info-border: #0369a1`

**Badges:**
- `--color-badge-purple-bg: #2e1065`, `--color-badge-purple-text: #c084fc`
- `--color-badge-blue-bg: #1e3a5f`, `--color-badge-blue-text: #60a5fa`
- `--color-badge-amber-bg: #451a03`, `--color-badge-amber-text: #fbbf24`
- `--color-badge-emerald-bg: #052e16`, `--color-badge-emerald-text: #34d399`
- `--color-badge-slate-bg: #1e293b`, `--color-badge-slate-text: #94a3b8`
- `--color-accent-indigo: #818cf8`
- `--color-accent-indigo-light: #1e2448`

**Accents:**
- `--color-accent-navbar-start: #1e2030`
- `--color-accent-navbar-end: #252838`
- `--color-accent-pairing: #818cf8`
- `--color-accent-recon: #38bdf8`
- `--color-accent-orderblock: #fbbf24`
- `--color-accent-sync: #f7931a` (stays same)

**Table:**
- `--color-table-header-bg: #1e2030`
- `--color-table-row-even: #161822`
- `--color-table-row-hover: #252838`

**Gradients:**
- `--color-gradient-section-start: #1e2030`, `--color-gradient-section-end: #252838`
- `--color-gradient-success-start: #052e16`, `--color-gradient-success-end: #064e3b`
- `--color-gradient-navbar-start: #1e2030`, `--color-gradient-navbar-end: #252838`

**Status (brighter for dark bg):**
- `--color-status-open: #22c55e`
- `--color-status-partial: #fbbf24`
- `--color-status-closed: #94a3b8`
- `--color-status-cancelled: #ef4444`
- `--color-status-draft: #818cf8`
- `--color-status-locked: #fbbf24`
- `--color-status-executed: #22c55e`

**Misc:**
- `--color-link: #818cf8`, `--color-link-hover: #a5b4fc`
- `--color-button-bg: #1e2030`
- `--color-focus-ring: #818cf8`
- `--color-scrollbar-track: #161822`, `--color-scrollbar-thumb: #3e4458`

**Shadows:**
- `--shadow-card: 0 1px 3px rgba(0, 0, 0, 0.3), 0 4px 16px rgba(0, 0, 0, 0.2)`
- `--shadow-hover: 0 6px 16px rgba(0, 0, 0, 0.4)`
- `--shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.3)`

Add a comment at the top of the block: `/* Dark Mode -- defined but not active until Phase 11 enables theme toggle */`

IMPORTANT: Every token in `:root` MUST have a corresponding override in `[data-theme="dark"]`. Missing tokens will produce light-colored elements on a dark background in Phase 11.

After writing the dark block, verify by counting: the number of `--color-*` and `--shadow-*` lines in `[data-theme="dark"]` should match the number in `:root`.
  </action>
  <verify>
Run `grep -c '\-\-color-\|--shadow-' frontend/src/index.css` to see total token lines (should be roughly 2x the :root count -- once in :root, once in dark block).
Run `grep '\[data-theme="dark"\]' frontend/src/index.css` to confirm the dark block exists.
Run `npm run build` from frontend/ to verify no CSS syntax errors.
Verify the app in the browser at `http://localhost:5173` -- it must look EXACTLY the same as before (dark block is defined but not activated).
  </verify>
  <done>
[data-theme="dark"] block exists in index.css with dark values for every token defined in :root. Token count in dark block matches :root. App appearance is unchanged (dark theme not yet activated). npm run build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `grep -c '\-\-color-' frontend/src/index.css` shows ~140-160 lines (light + dark definitions)
2. `grep '#[0-9a-fA-F]' frontend/src/index.css` shows hex values ONLY inside `:root {}` and `[data-theme="dark"] {}` blocks
3. `npm run build` succeeds without CSS errors
4. App loads in browser and looks identical to before
</verification>

<success_criteria>
- :root contains ~72-82 CSS custom property definitions
- [data-theme="dark"] contains matching dark overrides for every token
- index.css global selectors (a, button, body) use var() instead of hardcoded hex
- npm run build passes
- App appearance is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/10-css-variable-foundation/10-01-SUMMARY.md`
</output>
