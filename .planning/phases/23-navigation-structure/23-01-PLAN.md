---
phase: 23-navigation-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.jsx
  - frontend/src/components/SymbolLayout.jsx
  - frontend/src/components/GlobalNav.jsx
  - frontend/src/components/Backtest.jsx
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "User sees Trading/Analyse/Bot/Admin sub-navigation when visiting Settings page at /s/BTCEUR/settings"
    - "User sees Trading/Analyse/Bot/Admin sub-navigation when visiting Status Dashboard at /s/BTCEUR/status"
    - "User sees Trading/Analyse/Bot/Admin sub-navigation when visiting Backtest page at /s/BTCEUR/backtest"
    - "Old bookmark /settings redirects to /s/{lastSymbol}/settings (fallback BTCEUR)"
    - "Old bookmark /backtest redirects to /s/{lastSymbol}/backtest (fallback BTCEUR)"
    - "Old bookmark /status redirects to /s/{lastSymbol}/status (fallback BTCEUR)"
    - "Backtest page pre-selects symbol dropdown from URL symbol"
    - "Changing symbol in Backtest dropdown navigates to /s/{newSymbol}/backtest"
    - "Backtest link appears in Bot group (not Analyse group)"
    - "API Docs link removed from sub-nav (stays in footer only)"
    - "Admin group contains Reconciliation, Settings, Status"
    - "Switching symbol in GlobalNav while on /s/BTCEUR/settings goes to /s/ETHEUR/settings"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Redirect routes for /settings, /backtest, /status + new child routes inside SymbolLayout"
      contains: "settings|backtest|status"
    - path: "frontend/src/components/SymbolLayout.jsx"
      provides: "Updated sub-nav with Bot group containing Backtest, Admin group containing Settings+Status, API Docs removed"
      contains: "subnav-group"
    - path: "frontend/src/components/GlobalNav.jsx"
      provides: "localStorage persistence of last-visited symbol for redirect fallback"
      contains: "localStorage"
    - path: "frontend/src/components/Backtest.jsx"
      provides: "URL-synced symbol selection using useParams + navigate"
      contains: "useParams\\|useNavigate"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "SymbolLayout child routes"
      via: "React Router nested Route elements"
      pattern: "<Route path=\"settings\""
    - from: "frontend/src/App.jsx"
      to: "Redirect routes"
      via: "Navigate elements for old bookmarks"
      pattern: "<Route path=\"/settings\""
    - from: "frontend/src/components/GlobalNav.jsx"
      to: "localStorage"
      via: "setItem on symbol change, getItem for redirect default"
      pattern: "localStorage\\.setItem"
    - from: "frontend/src/components/Backtest.jsx"
      to: "URL params"
      via: "useParams for initial symbol, navigate for dropdown changes"
      pattern: "useParams|navigate"
---

<objective>
Move Settings, Status Dashboard, and Backtest routes inside SymbolLayout so all pages display consistent sub-navigation. Add redirect routes for old bookmarks, restructure sub-nav groups (move Backtest to Bot, add Settings+Status to Admin, remove API Docs from nav), and sync Backtest symbol selection with URL.

Purpose: Foundation for v3.2 UX consistency -- every page shows the same navigation context.
Output: All pages render inside SymbolLayout with proper sub-nav groups visible.
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-navigation-structure/23-CONTEXT.md

@frontend/src/App.jsx
@frontend/src/components/SymbolLayout.jsx
@frontend/src/components/GlobalNav.jsx
@frontend/src/components/Backtest.jsx
@frontend/src/contexts/SymbolContext.jsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/SymbolLayout.jsx:
- Reads `useParams().symbol`, validates against `KNOWN_PAIRS`, provides `SymbolProvider`
- Renders `<Outlet />` for child routes
- Sub-nav uses `subnav-group` divs with `subnav-group-label` spans
- NavLinks use pattern: `<NavLink to={`/s/${symbol}/path`}>Label</NavLink>`
- Live price ticker is part of SymbolLayout (always visible)

From frontend/src/components/GlobalNav.jsx:
- `buildSymbolUrl(newSymbol, currentPathname)` preserves sub-path on symbol switch
- Pattern: `/s/BTCEUR/lots` + click ETHEUR → `/s/ETHEUR/lots`
- Already works for any sub-path including /settings, /status, /backtest

From frontend/src/App.jsx:
- SymbolLayout children are nested `<Route>` elements under `<Route path="/s/:symbol" element={<SymbolLayout />}>`
- Currently Settings, Backtest, StatusDashboard are top-level routes outside SymbolLayout

From frontend/src/contexts/SymbolContext.jsx:
- `useSymbol()` throws if used outside SymbolProvider
- Settings/StatusDashboard do NOT call useSymbol() (they use useUser() only)
- Backtest does NOT call useSymbol() (it manages its own SYMBOLS constant + dropdown state)

From frontend/src/components/Backtest.jsx:
- Has `SYMBOLS` constant: `[{value:'BTCEUR', label:'BTC/EUR'}, {value:'XRPEUR', label:'XRP/EUR'}]`
- Symbol state managed via `useState` (not URL-aware)
- Does not import useParams or useNavigate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure routes and add redirects with localStorage symbol memory</name>
  <files>frontend/src/App.jsx, frontend/src/components/GlobalNav.jsx</files>
  <action>
**App.jsx changes:**

1. Move the three top-level routes (`/settings`, `/backtest`, `/status`) inside the SymbolLayout children:
   - Add `<Route path="settings" element={<Settings />} />`
   - Add `<Route path="backtest" element={<Backtest />} />`
   - Add `<Route path="status" element={<StatusDashboard />} />`
   These go alongside the existing children (dashboard, lots, combined, etc.)

2. Replace the removed top-level routes with redirect routes. Create a small helper component `SymbolRedirect` inside App.jsx (not exported, local to file):
   ```jsx
   function SymbolRedirect({ subPath }) {
     const lastSymbol = localStorage.getItem('cashmgnt_last_symbol') || 'BTCEUR';
     return <Navigate to={`/s/${lastSymbol}/${subPath}`} replace />;
   }
   ```
   Add three redirect routes:
   - `<Route path="/settings" element={<SymbolRedirect subPath="settings" />} />`
   - `<Route path="/backtest" element={<SymbolRedirect subPath="backtest" />} />`
   - `<Route path="/status" element={<SymbolRedirect subPath="status" />} />`

**GlobalNav.jsx changes:**

3. In `handleSymbolClick`, add `localStorage.setItem('cashmgnt_last_symbol', sym)` before the `navigate()` call so the last-visited symbol is remembered.

4. Also persist the current symbol on first detection: Add a `useEffect` that runs when `currentSymbol` changes — if `currentSymbol` is truthy, set `localStorage.setItem('cashmgnt_last_symbol', currentSymbol)`. This handles cases where user arrives via direct URL (not via click).

Key: The localStorage key is `cashmgnt_last_symbol`. Fallback value when missing: `'BTCEUR'`.
  </action>
  <verify>
    <automated>cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>Settings, Backtest, StatusDashboard routes are children of SymbolLayout. Old bookmark URLs (/settings, /backtest, /status) redirect to /s/{lastSymbol}/... with localStorage fallback to BTCEUR. GlobalNav persists last-visited symbol to localStorage. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure sub-nav groups and sync Backtest symbol with URL</name>
  <files>frontend/src/components/SymbolLayout.jsx, frontend/src/components/Backtest.jsx</files>
  <action>
**SymbolLayout.jsx changes:**

1. Restructure the sub-nav groups per user decisions:

   **Analyse group** (remove Backtest link):
   ```jsx
   <div className="subnav-group">
     <span className="subnav-group-label">Analyse</span>
     <NavLink to={`/s/${symbol}/combined`}>Combined Score</NavLink>
     <NavLink to={`/s/${symbol}/orderblock`}>Orderblocks</NavLink>
   </div>
   ```

   **Bot group** (add Backtest):
   ```jsx
   <div className="subnav-group">
     <span className="subnav-group-label">Bot</span>
     <NavLink to={`/s/${symbol}/bot`} className="bot-nav-link">
       Bot Dashboard
       {isDryRunActive && <span className="bot-nav-badge" />}
     </NavLink>
     <NavLink to={`/s/${symbol}/bot/decisions`}>Decision Log</NavLink>
     <NavLink to={`/s/${symbol}/backtest`}>Backtest</NavLink>
   </div>
   ```

   **Admin group** (add Settings + Status, remove API Docs link):
   ```jsx
   <div className="subnav-group">
     <span className="subnav-group-label">Admin</span>
     <NavLink to={`/s/${symbol}/reconciliation`}>Reconciliation</NavLink>
     <NavLink to={`/s/${symbol}/settings`}>Settings</NavLink>
     <NavLink to={`/s/${symbol}/status`}>Status</NavLink>
   </div>
   ```

   Remove the `<a href="/docs" ...>API Docs</a>` from the Admin group entirely — it already exists in the footer.

2. All NavLinks for Settings, Status, Backtest must use the symbol-relative pattern (`/s/${symbol}/path`), NOT absolute paths like `/settings`. This ensures `NavLink isActive` highlighting works correctly.

**Backtest.jsx changes:**

3. Add imports at top: `import { useParams, useNavigate } from 'react-router-dom';`

4. Inside the Backtest component, add:
   ```jsx
   const { symbol: urlSymbol } = useParams();
   const navigate = useNavigate();
   ```

5. Change the symbol state initialization to use URL symbol:
   - Replace `useState(SYMBOLS[0].value)` (or however it initializes) with `useState(urlSymbol || 'BTCEUR')`
   - The `urlSymbol` comes from the SymbolLayout parent route, so it will be e.g. `'BTCEUR'` or `'XRPEUR'`

6. When the user changes the symbol dropdown, navigate to the new URL:
   - Find the symbol change handler (the `onChange` of the symbol select/dropdown)
   - After setting local state, call `navigate(`/s/${newSymbol}/backtest`)` to keep URL in sync
   - This also triggers SymbolLayout to update its live price ticker

7. Keep the existing SYMBOLS constant and dropdown — do NOT remove the inline symbol selector. It serves as a quick-switch within the page (per user decision: "Keep the Backtest symbol dropdown as inline quick-switch").
  </action>
  <verify>
    <automated>cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>Sub-nav shows 4 groups: Trading (Dashboard, TradeLots), Analyse (Combined Score, Orderblocks), Bot (Bot Dashboard, Decision Log, Backtest), Admin (Reconciliation, Settings, Status). No API Docs link in sub-nav. Backtest pre-selects symbol from URL. Changing Backtest dropdown navigates to /s/{newSymbol}/backtest. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npx vite build --mode development` — must succeed with no errors
2. Grep verification:
   - `grep -n 'path="settings"' frontend/src/App.jsx` — shows settings as SymbolLayout child AND as redirect
   - `grep -n 'path="backtest"' frontend/src/App.jsx` — shows backtest as SymbolLayout child AND as redirect
   - `grep -n 'path="status"' frontend/src/App.jsx` — shows status as SymbolLayout child AND as redirect
   - `grep -n 'cashmgnt_last_symbol' frontend/src/components/GlobalNav.jsx` — shows localStorage usage
   - `grep -n 'useParams\|useNavigate' frontend/src/components/Backtest.jsx` — shows URL sync imports
   - `grep -c 'subnav-group' frontend/src/components/SymbolLayout.jsx` — should show 4 groups (Trading, Analyse, Bot, Admin)
   - `grep 'API Docs' frontend/src/components/SymbolLayout.jsx` — should return nothing (removed from sub-nav)
   - `grep 'API Docs' frontend/src/App.jsx` — should return match (still in footer)
</verification>

<success_criteria>
- All pages (Dashboard, TradeLots, Combined Score, Orderblocks, Bot Dashboard, Decision Log, Backtest, Reconciliation, Settings, Status) render inside SymbolLayout with consistent sub-navigation
- Sub-nav has 4 groups: Trading, Analyse, Bot, Admin — matching user decisions exactly
- Old bookmarks (/settings, /backtest, /status) redirect correctly
- Backtest symbol syncs bidirectionally with URL
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-navigation-structure/23-01-SUMMARY.md`
</output>
