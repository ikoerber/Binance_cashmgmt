---
phase: 13-xrpbtc-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/src/utils/symbolRegistry.js
  - frontend/src/components/GlobalNav.jsx
  - frontend/src/components/SymbolLayout.jsx
  - frontend/src/components/LotsTable.jsx
  - frontend/src/components/LotSummaryCards.jsx
  - frontend/src/components/Dashboard.jsx
  - frontend/src/index.css
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "XRPBTC pill appears after XRPEUR in navbar with amber/orange accent color"
    - "XRPBTC lots display both BTC and EUR values side by side (e.g. '0.00002345 BTC / 1.87 EUR')"
    - "Pair badge shows 'XRPBTC' or 'XRPEUR' on each lot row to distinguish visually"
    - "PairingPanel, checkboxes, and order creation buttons are hidden on XRPBTC page"
    - "XRPBTC dashboard shows subtle 'Low liquidity' indicator"
    - "Unrealized P&L uses live BTCEUR price for EUR conversion (not historical rate)"
  artifacts:
    - path: "frontend/src/utils/symbolRegistry.js"
      provides: "XRPBTC in KNOWN_PAIRS with BTC quote, isEurQuoted, isPairingEnabled, isOrderCreationEnabled helpers"
      contains: "XRPBTC"
    - path: "frontend/src/components/GlobalNav.jsx"
      provides: "Amber-styled XRPBTC pill in symbol selector"
      contains: "xrpbtc"
    - path: "frontend/src/components/LotsTable.jsx"
      provides: "Dual BTC/EUR display, hidden pairing UI and order buttons for XRPBTC"
      contains: "isEurQuoted"
    - path: "frontend/src/components/SymbolLayout.jsx"
      provides: "BTC quote label in live price display for XRPBTC"
      contains: "getQuoteDecimals"
  key_links:
    - from: "frontend/src/components/LotsTable.jsx"
      to: "frontend/src/utils/symbolRegistry.js"
      via: "isEurQuoted(activeSymbol) to branch dual display and hide pairing"
      pattern: "isEurQuoted"
    - from: "frontend/src/components/GlobalNav.jsx"
      to: "frontend/src/utils/symbolRegistry.js"
      via: "getAllSymbols() iterates KNOWN_PAIRS including XRPBTC"
      pattern: "getAllSymbols"
    - from: "frontend/src/components/SymbolLayout.jsx"
      to: "frontend/src/utils/symbolRegistry.js"
      via: "getQuoteDecimals(symbol) returns 8 for BTC-quoted pairs"
      pattern: "getQuoteDecimals"
---

<objective>
Add XRPBTC to frontend symbol registry, style the navbar pill with amber accent, implement dual BTC/EUR display in lot table, hide pairing/order UI for XRPBTC, and add low-liquidity indicator to dashboard.

Purpose: Users can navigate to XRPBTC, see lots with both BTC and EUR values, and understand that pairing and manual orders are not available for this pair.
Output: Complete frontend XRPBTC experience with dual display, pairing isolation, and visual distinction.
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-xrpbtc-infrastructure/13-CONTEXT.md
@.planning/phases/13-xrpbtc-infrastructure/13-RESEARCH.md
@.planning/phases/13-xrpbtc-infrastructure/13-01-SUMMARY.md
@frontend/src/utils/symbolRegistry.js
@frontend/src/utils/formatters.js
@frontend/src/components/GlobalNav.jsx
@frontend/src/components/SymbolLayout.jsx
@frontend/src/components/LotsTable.jsx
@frontend/src/components/LotSummaryCards.jsx
@frontend/src/components/Dashboard.jsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Symbol Registry + GlobalNav + SymbolLayout</name>
  <files>
    frontend/src/utils/symbolRegistry.js
    frontend/src/components/GlobalNav.jsx
    frontend/src/components/SymbolLayout.jsx
    frontend/src/index.css
  </files>
  <action>
1. **symbolRegistry.js** -- Add XRPBTC to KNOWN_PAIRS (after XRPEUR per user decision):
   ```javascript
   XRPBTC: { symbol: 'XRPBTC', base: 'XRP', quote: 'BTC', baseDecimals: 2, label: 'XRP/BTC' },
   ```
   Add helper functions:
   - `export const isEurQuoted = (symbol) => KNOWN_PAIRS[symbol]?.quote === 'EUR';`
   - `export const isPairingEnabled = (symbol) => isEurQuoted(symbol);`
   - `export const isOrderCreationEnabled = (symbol) => isEurQuoted(symbol);`
   Fix `getQuoteDecimals` to be symbol-aware (currently hardcoded to 2):
   ```javascript
   export const getQuoteDecimals = (symbol) => KNOWN_PAIRS[symbol]?.quote === 'BTC' ? 8 : 2;
   ```

2. **GlobalNav.jsx** -- Add amber/orange accent color for XRPBTC pill. The existing `getAllSymbols().map()` renders pills. Add a CSS class for BTC-quoted pairs:
   ```javascript
   <button
     key={sym}
     className={`symbol-pill ${sym === currentSymbol ? 'active' : ''} ${!isEurQuoted(sym) ? 'btc-quoted' : ''}`}
     onClick={() => handleSymbolClick(sym)}
   >
     {getPairLabel(sym)}
   </button>
   ```
   Import `isEurQuoted` from symbolRegistry.

3. **index.css** -- Add CSS for the amber XRPBTC pill. Use the existing Binance-accent color (#f7931a) per user decision to align with sync operations:
   - `.symbol-pill.btc-quoted` -- amber/orange border or background accent when not active
   - `.symbol-pill.btc-quoted.active` -- amber/orange active state (distinct from purple EUR pills)
   - Use CSS custom properties where possible: define `--color-btc-accent` as `#f7931a` in `:root` and appropriate dark mode value in `[data-theme="dark"]`
   - Add `.low-liquidity-badge` styles: subtle, inline badge with amber background, small text, rounded

4. **SymbolLayout.jsx** -- Update `getQuoteDecimals` call to pass symbol (it's now symbol-aware). The live price display already uses `getQuoteAsset()` and `getQuoteLabel()` which will correctly show "BTC" for XRPBTC. Verify the EUR symbol rendering: current code shows euro sign when `getQuoteAsset(symbol) === 'EUR'`, otherwise shows `getQuoteLabel(symbol)` -- this already handles XRPBTC correctly (will show "BTC" instead of euro sign). No functional change needed beyond the `getQuoteDecimals(symbol)` parameter fix.
  </action>
  <verify>
    Run: `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npm run build` -- build succeeds with no errors. Verify symbolRegistry: open dev tools, navigate to `/s/XRPBTC` and confirm the page loads (not redirected to BTCEUR).
  </verify>
  <done>XRPBTC appears as 4th pill in navbar (BTCEUR | ETHEUR | XRPEUR | XRPBTC) with amber accent color. Navigating to /s/XRPBTC shows the SymbolLayout with correct BTC quote formatting. getQuoteDecimals returns 8 for BTC-quoted pairs.</done>
</task>

<task type="auto">
  <name>Task 2: LotsTable Dual Display + Pairing Isolation + Dashboard</name>
  <files>
    frontend/src/components/LotsTable.jsx
    frontend/src/components/LotSummaryCards.jsx
    frontend/src/components/Dashboard.jsx
  </files>
  <action>
1. **LotsTable.jsx** -- Three changes:

   a) **Dual BTC/EUR display** -- For BTC-quoted pairs, show both values in cost, break-even, and P&L columns. Import `isEurQuoted` from symbolRegistry. At top of component: `const isEurPair = isEurQuoted(activeSymbol);`

   For cost column (currently shows `formatQuote(lot.cost_quote, activeSymbol)`):
   ```jsx
   <td>
     {formatQuote(lot.cost_quote, activeSymbol)}
     {!isEurPair && lot.cost_eur && (
       <span className="eur-equivalent"> / {formatEUR(parseFloat(lot.cost_eur))}</span>
     )}
   </td>
   ```
   Same pattern for break_even column: show `break_even` (native BTC price) and `break_even_eur` side by side.
   Same pattern for P&L column: show BTC P&L and EUR-equivalent P&L. For unrealized P&L EUR conversion, use live BTCEUR price (per user decision: "Unrealized P&L uses live BTCEUR price for EUR conversion"). Get the live BTCEUR price from the WebSocket context prices map.

   Import `formatEUR` from formatters.js. Add `.eur-equivalent` CSS class in LotsTable.css: same font-size, slightly muted color (use `var(--color-text-muted)`).

   b) **Pair badge** -- Add a small badge on each lot row showing the pair symbol. Per user decision: "Pair badge on each lot row: small badge showing 'XRPBTC' or 'XRPEUR'". This helps distinguish lots visually when viewing a mixed context. Add the badge next to the order ID:
   ```jsx
   <span className={`pair-badge ${!isEurQuoted(lot.symbol) ? 'pair-badge-btc' : ''}`}>
     {getPairLabel(lot.symbol)}
   </span>
   ```
   CSS: `.pair-badge` small rounded badge with neutral background. `.pair-badge-btc` amber tint.

   c) **Hide pairing/order UI** -- Import `isPairingEnabled, isOrderCreationEnabled` from symbolRegistry. Gate:
   - Checkboxes column: `{isPairingEnabled(activeSymbol) && <th className="checkbox-col">...` and same for td cells
   - Pairing toggle button: `{isPairingEnabled(activeSymbol) && <button className="btn-pairing-toggle"...`
   - PairingPanel: `{isPairingEnabled(activeSymbol) && pairingPanelOpen && <PairingPanel...`
   - Order creation button (the "+ Order" or create order button in the row): `{isOrderCreationEnabled(activeSymbol) && ...}`
   - The "Select all" checkbox header: gated by isPairingEnabled

2. **LotSummaryCards.jsx** -- If this component shows recovery price or other EUR-specific KPIs, add dual display for BTC-quoted pairs. The recovery price calculation may need EUR conversion via live BTCEUR price. Import `isEurQuoted` and adapt display. If no EUR-specific KPIs apply to XRPBTC, the component works as-is (costs show in BTC).

3. **Dashboard.jsx** -- Add a subtle "Low liquidity" indicator for XRPBTC per user decision. At the top of the dashboard (below the CombinedScoreWidget or as a small info banner):
   ```jsx
   {!isEurQuoted(activeSymbol) && (
     <div className="low-liquidity-notice">
       <span className="low-liquidity-badge">Low Liquidity</span>
       <span>BTC-Paar -- eingeschraenkte Liquiditaet, kein Pairing/Orders</span>
     </div>
   )}
   ```
   Style: Amber background, subtle, not intrusive. Use existing `.info-notice` patterns or create a minimal banner.
  </action>
  <verify>
    Run: `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npm run build && npm run lint` -- build and lint pass. Visual verification: navigate to `/s/XRPBTC/lots` and confirm: (1) no checkboxes, (2) no pairing button, (3) cost/break-even show dual BTC/EUR, (4) pair badges visible. Navigate to `/s/XRPBTC/dashboard` and confirm low-liquidity notice appears.
  </verify>
  <done>XRPBTC lots display dual BTC/EUR values side by side. Pair badge shows on each lot row. PairingPanel, checkboxes, and order creation buttons are hidden for XRPBTC. Dashboard shows subtle "Low liquidity" indicator. Unrealized P&L converted to EUR using live BTCEUR price.</done>
</task>

</tasks>

<verification>
- Navigate to `/s/XRPBTC` -- page loads, not redirected
- XRPBTC pill in navbar has amber/orange accent color
- Live price shows BTC formatting (8 decimals) with "BTC" label (not EUR symbol)
- XRPBTC lots table: no checkboxes, no pairing button, no "+ Order" button
- Cost/break-even columns show dual display: BTC value / EUR value
- Pair badge visible on lot rows
- XRPBTC dashboard shows "Low liquidity" notice
- `npm run build` succeeds
- `npm run lint` passes
</verification>

<success_criteria>
Users can navigate to XRPBTC in the symbol selector, see lots with dual BTC/EUR display and pair badges, understand that pairing and orders are disabled (UI elements hidden), and see the low-liquidity notice on the dashboard. The amber XRPBTC pill visually distinguishes it from purple EUR pairs.
</success_criteria>

<output>
After completion, create `.planning/phases/13-xrpbtc-infrastructure/13-03-SUMMARY.md`
</output>
