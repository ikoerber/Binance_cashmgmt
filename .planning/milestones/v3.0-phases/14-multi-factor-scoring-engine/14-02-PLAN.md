---
phase: 14-multi-factor-scoring-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/alpha_score_settings.py
  - backend/app/db/models.py
  - backend/app/api/routes/settings.py
  - frontend/src/components/Settings.jsx
  - frontend/src/components/Settings.css
  - frontend/src/api/client.js
autonomous: true
requirements:
  - SCORE-06

must_haves:
  truths:
    - "User can see Alpha Score settings section in the Settings page with essential settings visible and advanced settings collapsed"
    - "User can configure factor weights (4), trade threshold, and candle interval as essential settings"
    - "User can expand advanced settings to configure rolling windows, ATR multipliers, Hurst lookback, regime thresholds, trailing stop resume threshold"
    - "User can click Reset to Defaults to restore all Alpha Score settings"
    - "Changes persist after save and are returned on GET /api/settings"
    - "Weights are auto-normalized to sum to 100% on save"
  artifacts:
    - path: "backend/app/db/models.py"
      provides: "UserSettingsDB with alpha_score_* columns"
      contains: "alpha_score_interval"
    - path: "backend/app/api/routes/settings.py"
      provides: "Extended Settings API with Alpha Score fields"
      contains: "alpha_score_weight_zscore"
    - path: "frontend/src/components/Settings.jsx"
      provides: "Alpha Score settings section with essential/advanced grouping"
      contains: "Alpha Score"
  key_links:
    - from: "frontend/src/components/Settings.jsx"
      to: "backend/app/api/routes/settings.py"
      via: "PUT /api/settings with alpha_score_* fields"
      pattern: "alpha_score"
    - from: "backend/app/api/routes/settings.py"
      to: "backend/app/db/models.py"
      via: "UserSettingsDB alpha_score_* columns"
      pattern: "alpha_score"
---

<objective>
Add Alpha Score configuration settings to the database, backend API, and frontend Settings page.

Purpose: Users must be able to configure Alpha Score weights, thresholds, and parameters before the scoring engine is wired up. This plan is independent of the domain logic and can be built in parallel with Plan 01 (factor computations).

Output: Alembic migration adding ~13 alpha_score_* columns to UserSettingsDB, extended Settings API route with validation, frontend Settings.jsx with essential/advanced grouped layout and reset-to-defaults button.
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-factor-scoring-engine/14-CONTEXT.md
@.planning/phases/14-multi-factor-scoring-engine/14-RESEARCH.md

@backend/app/db/models.py (UserSettingsDB class, existing columns pattern)
@backend/app/api/routes/settings.py (existing validation pattern, SettingsUpdate model, _settings_to_dict)
@frontend/src/components/Settings.jsx (existing form pattern, useQuery/useMutation)
@frontend/src/components/Settings.css (existing styles)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic Migration + UserSettingsDB Extension + Settings API</name>
  <files>
    backend/alembic/versions/alpha_score_settings.py
    backend/app/db/models.py
    backend/app/api/routes/settings.py
  </files>
  <action>
    1. Create Alembic migration adding these columns to `user_settings` (all nullable, defaults handled in code):
       - `alpha_score_interval` (String) -- "5m"|"15m"|"1h", default "15m"
       - `alpha_score_weight_zscore` (Numeric(5,2)) -- default 40
       - `alpha_score_weight_leadlag` (Numeric(5,2)) -- default 30
       - `alpha_score_weight_imbalance` (Numeric(5,2)) -- default 20
       - `alpha_score_weight_funding` (Numeric(5,2)) -- default 10
       - `alpha_score_threshold` (Numeric(5,2)) -- default 3.0
       - `alpha_score_zscore_window` (Numeric(5,0)) -- default 60
       - `alpha_score_leadlag_window` (Numeric(5,0)) -- default 30
       - `alpha_score_hurst_lookback` (Numeric(5,0)) -- default 100
       - `alpha_score_hurst_trending` (Numeric(5,4)) -- default 0.55
       - `alpha_score_hurst_reverting` (Numeric(5,4)) -- default 0.45
       - `alpha_score_atr_mult_btc` (Numeric(5,2)) -- default 2.0
       - `alpha_score_atr_mult_xrp` (Numeric(5,2)) -- default 3.0
       - `alpha_score_stop_resume_n` (Numeric(3,0)) -- default 5
       Use `render_as_batch=True` (SQLite). Use `alembic revision --autogenerate -m "add alpha score settings"` then verify the generated migration.

    2. Add matching Column definitions to `UserSettingsDB` in `backend/app/db/models.py`, grouped under a comment `# Alpha Score Settings`. Follow exact pattern of existing `ob_*` columns (Numeric, nullable=True).

    3. Extend `backend/app/api/routes/settings.py`:
       - Add alpha_score_* fields to `SettingsUpdate` Pydantic model (all Optional with defaults matching above)
       - Add `VALID_ALPHA_INTERVALS = {"5m", "15m", "1h"}`
       - Extend `_settings_to_dict()` to include all alpha_score_* fields with defaults
       - Extend `DEFAULTS` dict with alpha_score_* defaults
       - Add Decimal validation for weight/threshold/window fields in `update_settings()` following existing `ob_atr_multiplier` pattern:
         - Weights: 0-100 range
         - Threshold: 0.1-5.0 range
         - Windows: 10-500 range (integers)
         - Hurst thresholds: 0.0-1.0 range
         - ATR multipliers: 0.5-10.0 range
         - Resume N: 1-50 range
       - Auto-normalize weights on save: sum the 4 weight values, divide each by sum, multiply by 100. Store normalized values. This implements Claude's discretion on weight normalization.
       - Extend the upsert logic to set all alpha_score_* fields on both update and create paths

    Run `alembic upgrade head` to apply migration.
  </action>
  <verify>
    ```bash
    cd /Users/ikoerber/AIProjects/cashmgnt/backend
    source venv/bin/activate
    alembic upgrade head
    # Test GET returns defaults
    curl -s -H "X-API-Key: $API_KEY" http://localhost:8000/api/settings/user1 | python3 -m json.tool | grep alpha_score
    ```
    GET returns all alpha_score_* fields with defaults. Migration applied without errors.
  </verify>
  <done>
    UserSettingsDB has 14 new alpha_score_* columns. Settings API GET returns defaults, PUT validates and persists all alpha_score_* fields. Weight auto-normalization works on save.
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings Frontend - Alpha Score Configuration Section</name>
  <files>
    frontend/src/components/Settings.jsx
    frontend/src/components/Settings.css
    frontend/src/api/client.js
  </files>
  <action>
    1. Extend `frontend/src/api/client.js`: Ensure `updateSettings()` sends all alpha_score_* fields in the request body (they flow through existing getSettings/updateSettings functions, just need to include the new fields).

    2. Extend `frontend/src/components/Settings.jsx`:
       - Add state variables for all 14 alpha_score_* fields with default values
       - Populate from settings response in useEffect (same pattern as existing ob_* fields)
       - Add new section "Alpha Score" below existing sections, with two sub-sections:

       **Essential Settings (always visible):**
       - Candle Interval: button group (5m | 15m | 1h), same style as sell_allocation_strategy buttons
       - Factor Weights: 4 number inputs (Z-Score, Lead-Lag, Orderbook, Funding) with labels showing auto-normalized percentages. Display note: "Gewichte werden automatisch auf 100% normalisiert."
       - Trade Threshold: number input (+-3.0), label "Signal-Schwelle (Score >= Schwelle = Signal)"

       **Advanced Settings (collapsed by default):**
       - Collapsible section with toggle "Erweiterte Einstellungen"
       - Z-Score Window: number input (default 60)
       - Lead-Lag Window: number input (default 30)
       - Hurst Lookback: number input (default 100)
       - Hurst Trending Threshold: number input (default 0.55)
       - Hurst Mean-Reverting Threshold: number input (default 0.45)
       - ATR Multiplier BTC: number input (default 2.0)
       - ATR Multiplier XRP: number input (default 3.0)
       - Trailing Stop Resume: number input (default 5, label "Datenpunkte bis Wiederaufnahme")

       **Reset to Defaults button:**
       - Below the Alpha Score section
       - On click: resets all alpha_score_* state variables to defaults (not a server call, just local state reset that gets saved on next "Speichern")
       - Style: secondary button, slate color

       - Include all alpha_score_* fields in the save mutation payload
       - Follow existing patterns exactly: useState for each field, useEffect to populate, include in saveMutation payload

    3. Extend `frontend/src/components/Settings.css`:
       - `.settings-section-alpha` styling consistent with existing sections
       - `.settings-advanced-toggle` for the collapsible toggle
       - `.settings-advanced-content` with collapse/expand animation (simple max-height transition)
       - `.settings-weight-group` for the 4-weight input row
       - `.settings-weight-note` for the normalization hint text
       - `.settings-reset-btn` for the reset button
       - Use existing color palette: Indigo for Alpha Score accent (#6366f1), slate for secondary elements
  </action>
  <verify>
    ```bash
    cd /Users/ikoerber/AIProjects/cashmgnt/frontend
    npm run build
    ```
    Build succeeds. Manually verify: Settings page shows Alpha Score section, essential settings visible, advanced collapsed, reset button works.
  </verify>
  <done>
    Settings page has Alpha Score section with essential/advanced grouping. Users can configure all 14 Alpha Score parameters. Reset to Defaults button restores all values. Save persists to backend.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ikoerber/AIProjects/cashmgnt/backend
source venv/bin/activate
alembic upgrade head
pytest tests/ -v --tb=short  # Existing tests still pass
cd ../frontend && npm run build  # Frontend builds
```
</verification>

<success_criteria>
- Alembic migration adds 14 columns without error
- GET /api/settings returns all alpha_score_* fields with defaults
- PUT /api/settings validates and persists alpha_score_* fields
- Weight auto-normalization: weights always sum to 100% after save
- Frontend Settings page shows Alpha Score section with essential/advanced layout
- Reset to Defaults button works
- Existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-multi-factor-scoring-engine/14-02-SUMMARY.md`
</output>
