---
phase: 11-dark-mode-activation-charts
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - frontend/src/components/OrderblockChart.jsx
autonomous: true
requirements: [DARK-04]

must_haves:
  truths:
    - "Candlestick chart has a dark background (#161822) with clearly visible candles, grid lines, and axis labels"
    - "Volume histogram bars are visible on dark background (semi-transparent green/red with sufficient alpha)"
    - "Zone overlays (price lines, markers) are readable against dark chart background"
    - "Trade markers (entry, stop, target, exit) have sufficient contrast on dark background"
  artifacts:
    - path: "frontend/src/components/OrderblockChart.jsx"
      provides: "Dark-themed lightweight-charts candlestick chart with zone overlays"
      contains: "useChartTheme"
      min_lines: 200
  key_links:
    - from: "frontend/src/components/OrderblockChart.jsx"
      to: "frontend/src/hooks/useChartTheme.js"
      via: "import and call useChartTheme() for all chart colors"
      pattern: "import.*useChartTheme"
    - from: "frontend/src/components/OrderblockChart.jsx"
      to: "frontend/src/hooks/useChartTheme.js"
      via: "import hexToRgb for volume bar rgba() construction"
      pattern: "import.*hexToRgb"
---

<objective>
Convert OrderblockChart.jsx from hardcoded light-mode hex values to theme-aware colors via useChartTheme hook.

Purpose: The candlestick chart (lightweight-charts, canvas-based) currently uses 23 hardcoded hex values + 2 rgba values designed for white backgrounds. These must be replaced with dark-mode-aware values read from CSS custom properties via the useChartTheme hook created in Plan 01.
Output: Fully dark-themed candlestick chart with readable candles, volume bars, zone overlays, and trade markers.
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dark-mode-activation-charts/11-RESEARCH.md
@.planning/phases/11-dark-mode-activation-charts/11-01-SUMMARY.md
@frontend/src/components/OrderblockChart.jsx
@frontend/src/hooks/useChartTheme.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert OrderblockChart.jsx createChart config + series colors to theme values</name>
  <files>frontend/src/components/OrderblockChart.jsx</files>
  <action>
Import `useChartTheme` and `hexToRgb` from `'../hooks/useChartTheme'` at the top of the file.

Call `const theme = useChartTheme();` at the top of the component function body (before any useEffect/useRef).

Replace ALL 25 hardcoded color values (23 hex + 2 rgba) with theme-aware equivalents. The complete mapping:

**createChart() layout config:**
- `background: { type: ColorType.Solid, color: '#ffffff' }` --> `color: theme.bgCard`
- `textColor: '#64748b'` --> `theme.textSecondary`

**createChart() grid config:**
- `vertLines: { color: '#f1f5f9' }` --> `theme.borderLight`
- `horzLines: { color: '#f1f5f9' }` --> `theme.borderLight`

**createChart() scale config:**
- `rightPriceScale: { borderColor: '#e2e8f0' }` --> `theme.border`
- `timeScale: { borderColor: '#e2e8f0' }` --> `theme.border`

**Candlestick series options:**
- `upColor: '#16a34a'` --> `theme.profit`
- `downColor: '#dc2626'` --> `theme.loss`
- `borderUpColor: '#16a34a'` --> `theme.profit`
- `borderDownColor: '#dc2626'` --> `theme.loss`
- `wickUpColor: '#16a34a'` --> `theme.profit`
- `wickDownColor: '#dc2626'` --> `theme.loss`

**Volume histogram series:**
- `color: '#94a3b8'` --> `theme.textMuted`

**Volume data map (per-bar colors):**
- `'rgba(22,163,74,0.25)'` --> `` `rgba(${hexToRgb(theme.profit)}, 0.3)` `` (increased alpha from 0.25 to 0.3 for dark background visibility)
- `'rgba(220,38,38,0.25)'` --> `` `rgba(${hexToRgb(theme.loss)}, 0.3)` `` (increased alpha from 0.25 to 0.3)

**Zone overlay (addPriceLine / createShape calls):**
- `const zoneBorder = isBullish ? '#16a34a' : '#dc2626'` --> `const zoneBorder = isBullish ? theme.profit : theme.loss`
- Equilibrium line `color: '#d97706'` --> `theme.accentOrderblock`
- Sweep level `color: '#f59e0b'` --> `theme.accentAmber`
- OB marker `color: '#d97706'` --> `theme.accentOrderblock`
- BOS marker `color: '#d97706'` --> `theme.accentOrderblock`

**Trade markers:**
- Entry line `color: '#3b82f6'` --> `theme.accentBlue`
- Stop line `color: '#dc2626'` --> `theme.loss`
- Target line `color: '#16a34a'` --> `theme.profit`
- Entry marker `color: '#3b82f6'` --> `theme.accentBlue`
- Exit marker (conditional) `color: isHit ? '#16a34a' : '#dc2626'` --> `color: isHit ? theme.profit : theme.loss`

**IMPORTANT:** The `theme` object values are read once when the component renders. Since the app is dark-only (no toggle), there is no need for a useEffect to re-apply theme on change. The createChart useEffect should use `theme` values directly in the config objects.

Add `theme` to the useEffect dependency array where the chart is created, so if theme values were to change (future toggle), the chart would recreate. This is forward-compatible.

After conversion, verify zero hardcoded hex remain: `grep -n '#[0-9a-fA-F]\{3,8\}' frontend/src/components/OrderblockChart.jsx` should return 0 lines (excluding comments and the ColorType import).
  </action>
  <verify>
1. `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/components/OrderblockChart.jsx` returns 0 (no hardcoded hex in string literals)
2. `grep -c 'rgba(' frontend/src/components/OrderblockChart.jsx` returns 0 for old hardcoded rgba (the new ones use template literals with hexToRgb)
3. `grep -c 'useChartTheme' frontend/src/components/OrderblockChart.jsx` returns at least 2 (import + call)
4. `grep -c 'hexToRgb' frontend/src/components/OrderblockChart.jsx` returns at least 2 (import + usage)
5. `cd frontend && npm run build` succeeds
  </verify>
  <done>OrderblockChart.jsx has zero hardcoded hex/rgba values. All 25 color instances replaced with theme-aware values from useChartTheme hook. Volume bar alpha increased to 0.3 for dark background visibility. Build passes.</done>
</task>

</tasks>

<verification>
1. Open Orderblock page in browser, run an analysis to generate zones
2. Click a zone to open the candlestick chart -- chart background is dark, candles are green/red on dark background
3. Grid lines are subtle but visible (dark border color, not bright white)
4. Volume histogram bars are visible (semi-transparent green/red, not invisible)
5. Zone overlays (price lines for top/bottom, equilibrium, entry/stop/target) are clearly readable
6. Markers (OB, BOS, entry, exit) have good contrast
7. Tooltip/crosshair works correctly on dark background
8. `cd frontend && npm run build` passes
</verification>

<success_criteria>
- Zero hardcoded hex or rgba values in OrderblockChart.jsx
- Candlestick chart renders correctly on dark background
- All zone overlays, trade markers, and volume bars visible with sufficient contrast
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-dark-mode-activation-charts/11-02-SUMMARY.md`
</output>
