---
phase: 12-navigation-restructure-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/CombinedScoreWidget.jsx
  - frontend/src/components/CombinedScoreWidget.css
  - frontend/src/components/Dashboard.jsx
  - frontend/src/components/Dashboard.css
autonomous: true
requirements:
  - NAV-02

must_haves:
  truths:
    - "CombinedScoreWidget renders action label, multiplier, and score bar independently from CombinedScore page"
    - "Dashboard shows Combined Score hero widget above the depot-flow KPI section"
    - "Clicking the CombinedScoreWidget navigates to the full Combined Score detail page"
  artifacts:
    - path: "frontend/src/components/CombinedScoreWidget.jsx"
      provides: "Compact Combined Score hero banner widget"
      min_lines: 40
    - path: "frontend/src/components/CombinedScoreWidget.css"
      provides: "Widget-specific styling (compact banner, clickable card)"
      min_lines: 20
    - path: "frontend/src/components/Dashboard.jsx"
      provides: "Per-symbol dashboard with CombinedScoreWidget embedded"
      contains: "CombinedScoreWidget"
  key_links:
    - from: "frontend/src/components/CombinedScoreWidget.jsx"
      to: "/api/combined/{user_id}/score"
      via: "useQuery with getCombinedScore"
      pattern: "getCombinedScore"
    - from: "frontend/src/components/Dashboard.jsx"
      to: "frontend/src/components/CombinedScoreWidget.jsx"
      via: "import and render"
      pattern: "import CombinedScoreWidget"
    - from: "frontend/src/components/CombinedScoreWidget.jsx"
      to: "/s/:symbol/combined"
      via: "useNavigate onClick"
      pattern: "navigate.*combined"
---

<objective>
Create the Combined Score hero widget and embed it in the per-symbol Dashboard.

Purpose: The central trading recommendation (action + multiplier + score bar) must be immediately visible when a user selects a symbol, without navigating to the full Combined Score detail page. This is the key UX improvement of Phase 12.

Output: CombinedScoreWidget.jsx + .css (new), Dashboard.jsx updated with widget embedded above depot-flow KPIs.
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-navigation-restructure-dashboard/12-RESEARCH.md
@frontend/src/components/CombinedScore.jsx
@frontend/src/components/CombinedScore.css
@frontend/src/components/Dashboard.jsx
@frontend/src/components/Dashboard.css
@frontend/src/hooks/useChartTheme.js
@frontend/src/api/client.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CombinedScoreWidget component</name>
  <files>
    frontend/src/components/CombinedScoreWidget.jsx
    frontend/src/components/CombinedScoreWidget.css
  </files>
  <action>
Create a compact CombinedScoreWidget.jsx that extracts the hero banner section from CombinedScore.jsx (lines 89-132). The widget must:

1. **Own data fetching:** Use its own useQuery with queryKey `['combined-score', symbol, userId, interval]` -- same key pattern as CombinedScore.jsx so TanStack Query deduplicates and caches automatically. Fetch settings first for interval, then fetch combined score with getCombinedScore.

2. **Render the hero banner compactly:**
   - Action label (e.g., "Kaufen", "Aggressiv kaufen") with action color
   - Size multiplier (e.g., "1.20x") with profit/loss/muted color
   - Unified Score bar with threshold marks and score indicator (same as CombinedScore.jsx lines 101-131)
   - Score value and intensity text below the bar

3. **Use theme-aware colors:** Import useChartTheme from hooks/useChartTheme. Use the ACTION_COLOR_MAP pattern (German action strings as keys) from CombinedScore.jsx lines 38-46 for action colors. Use theme.profit/theme.loss/theme.textMuted for multiplier color.

4. **Click-to-navigate:** The entire widget is clickable. Use useNavigate from react-router-dom to navigate to `/s/${symbol}/combined` on click. Show a subtle "Details anzeigen" hint text.

5. **Loading/error states:** Show a compact loading skeleton (single line, not full-page). On error, show a muted error line. On no data, render null.

6. **Import pattern:** Follow project conventions:
   ```
   import { useQuery } from '@tanstack/react-query';
   import { useNavigate } from 'react-router-dom';
   import { getCombinedScore, getSettings } from '../api/client';
   import { useChartTheme } from '../hooks/useChartTheme';
   import { useSymbol } from '../contexts/SymbolContext';
   import { useUser } from '../contexts/UserContext';
   import { formatNumber } from '../utils/formatters';
   import './CombinedScoreWidget.css';
   ```

Create CombinedScoreWidget.css with:
- `.combined-widget` -- card styling using CSS variables only (var(--color-bg-card), var(--shadow-card), var(--radius-lg)). Clickable: cursor pointer, subtle hover effect (translateY(-1px) + shadow increase). Border-top with var(--color-accent-combined).
- `.combined-widget-loading` / `.combined-widget-error` -- compact single-line states
- Reuse the combined-score-bar classes from CombinedScore.css where possible by prefixing widget-specific classes (`.widget-action-banner`, `.widget-score-bar-section`, etc.)
- `.widget-details-hint` -- small muted text at bottom right ("Details anzeigen >")
- All colors MUST use CSS custom properties from index.css -- zero hardcoded hex values (Dark Mode requirement from Phase 10/11).
  </action>
  <verify>
Run `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npx vite build 2>&1 | tail -20` to verify no import/syntax errors. Grep for hardcoded hex: `grep -n '#[0-9a-fA-F]\{3,8\}' frontend/src/components/CombinedScoreWidget.css frontend/src/components/CombinedScoreWidget.jsx` should return zero matches.
  </verify>
  <done>
CombinedScoreWidget.jsx renders action label + multiplier + score bar, fetches its own data via useQuery with same queryKey as CombinedScore.jsx (cache sharing), navigates to combined page on click. Zero hardcoded hex values in JSX and CSS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Embed CombinedScoreWidget in Dashboard</name>
  <files>
    frontend/src/components/Dashboard.jsx
    frontend/src/components/Dashboard.css
  </files>
  <action>
Update Dashboard.jsx to embed the CombinedScoreWidget above the depot-flow section:

1. **Add import:** `import CombinedScoreWidget from './CombinedScoreWidget';` after existing imports.

2. **Insert widget:** Place `<CombinedScoreWidget />` between the `<h1>` heading and the `<div className="depot-flow">` section. The widget provides its own context access (useSymbol, useUser) so no props needed.

3. **Add spacing:** In Dashboard.css, add a margin-bottom to `.combined-widget` when inside `.dashboard` context: `.dashboard .combined-widget { margin-bottom: 24px; }` -- or add a wrapper div with appropriate spacing.

4. **Do NOT change** any existing Dashboard functionality (portfolio query, daily performance, depot-flow KPIs). The widget is purely additive.

5. **Verify contexts:** Dashboard.jsx already uses `useSymbol()` and `useUser()` -- these are provided by SymbolLayout (parent route). CombinedScoreWidget uses the same contexts. No context nesting issues.
  </action>
  <verify>
Run `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npx vite build 2>&1 | tail -20` -- build must succeed. Verify the import exists: `grep 'CombinedScoreWidget' frontend/src/components/Dashboard.jsx`.
  </verify>
  <done>
Dashboard.jsx renders CombinedScoreWidget above the depot-flow KPI cards. Build succeeds. Widget is purely additive -- all existing Dashboard functionality (portfolio, daily performance) unchanged.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vite build` -- production build succeeds with no errors
2. CombinedScoreWidget.jsx exists with useQuery fetching combined score data
3. Dashboard.jsx imports and renders CombinedScoreWidget
4. Zero hardcoded hex values in new files: `grep -rn '#[0-9a-fA-F]\{3,8\}' frontend/src/components/CombinedScoreWidget.*`
5. Same queryKey pattern as CombinedScore.jsx ensures TanStack Query cache sharing
</verification>

<success_criteria>
- CombinedScoreWidget.jsx is a self-contained component with own data fetching
- Dashboard.jsx shows the widget above depot-flow KPIs
- Widget displays: action label, multiplier, score bar with thresholds
- Clicking widget navigates to /s/:symbol/combined
- All styling uses CSS custom properties (dark-mode compatible)
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-restructure-dashboard/12-01-SUMMARY.md`
</output>
