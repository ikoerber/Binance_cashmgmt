---
phase: 09-xrpbtc-removal
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/utils/symbolRegistry.js
  - frontend/src/api/client.js
  - frontend/src/components/PairingPanel.jsx
  - frontend/src/components/PairingExistingTab.jsx
  - frontend/src/components/SimulationModal.jsx
  - frontend/src/components/PairingPanel.css
  - frontend/src/hooks/useLotsData.js
  - frontend/src/components/Overview.jsx
  - frontend/src/components/Dashboard.jsx
  - frontend/src/components/Orderblock.jsx
  - frontend/src/components/OrderblockZoneTable.jsx
  - frontend/src/components/SymbolLayout.jsx
autonomous: true
requirements: [REM-06]

must_haves:
  truths:
    - "Frontend symbol registry has no XRPBTC entry"
    - "getSymbolsForBaseAsset() does not exist in frontend code"
    - "No cross-pair toggle, pair-origin badges, or routing-decision display visible in UI"
    - "Pairing API calls have no base_asset, xrpbtcPrice, or btceurPrice parameters"
    - "SimulationModal shows no dual-route comparison section"
    - "PairingPanel.css has no routing-badge, cross-pair-toggle, pair-origin-badge styles"
  artifacts:
    - path: "frontend/src/utils/symbolRegistry.js"
      provides: "3-symbol EUR-only registry without getSymbolsForBaseAsset()"
    - path: "frontend/src/api/client.js"
      provides: "Simplified API client without cross-pair parameters"
    - path: "frontend/src/components/PairingPanel.jsx"
      provides: "Pairing panel without cross-pair toggle or cross-pair mode"
    - path: "frontend/src/components/PairingExistingTab.jsx"
      provides: "Existing pairings tab without routing badges or pair-origin badges"
    - path: "frontend/src/components/PairingPanel.css"
      provides: "CSS without cross-pair styles"
  key_links:
    - from: "frontend/src/components/PairingPanel.jsx"
      to: "frontend/src/api/client.js"
      via: "getPairingSuggestions/createPairing calls without base_asset"
      pattern: "getPairingSuggestions|createPairing"
    - from: "frontend/src/api/client.js"
      to: "backend API"
      via: "Simplified pairing API calls matching Plan 01 backend"
      pattern: "pairing.*suggestions|pairing.*create|pairing.*simulate"
---

<objective>
Remove all XRPBTC and cross-pair UI elements from the frontend: Symbol Registry, API client, Pairing components, CSS styles, and cosmetic comments.

Purpose: Frontend matches the simplified backend API (Plan 01). No cross-pair UI elements visible. Clean code without XRPBTC references.
Output: Frontend with 3-symbol EUR-only registry, simplified pairing UI, no routing/cross-pair visuals.
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-xrpbtc-removal/09-CONTEXT.md
@.planning/phases/09-xrpbtc-removal/09-RESEARCH.md
@.planning/phases/09-xrpbtc-removal/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove XRPBTC from Frontend Symbol Registry and API Client</name>
  <files>
    frontend/src/utils/symbolRegistry.js
    frontend/src/api/client.js
  </files>
  <action>
**symbolRegistry.js:**
- Remove XRPBTC entry from KNOWN_PAIRS object. Only BTCEUR, ETHEUR, XRPEUR remain.
- DELETE `getSymbolsForBaseAsset()` function entirely (no multi-symbol per base asset needed).
- DELETE `hasMultipleSymbols()` function if it exists (only used to decide cross-pair toggle visibility).
- Simplify `getQuoteDecimals()` -- since only EUR-quoted pairs remain, it can always return 2. Remove the BTC branch.
- Keep all other functions (getSymbolConfig, getBaseDecimals, getPairLabel, etc.).

**api/client.js:**
- From `simulatePairing()`: Remove `xrpbtcPrice` and `btceurPrice` parameters. Remove these from the request params/body sent to the backend.
- From `getPairingSuggestions()`: Remove `baseAsset` parameter. Remove from request params.
- From `createPairing()`: Remove `baseAsset` parameter. Remove from request body.
- From `listPairings()`: Remove `baseAsset` filter parameter if present.
- Clean up any other XRPBTC-specific parameters or comments across the file.
  </action>
  <verify>
Run `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -r 'XRPBTC' src/utils/symbolRegistry.js` -- should return zero results.
Run `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -r 'getSymbolsForBaseAsset\|hasMultipleSymbols' src/` -- should return zero results.
Run `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -r 'xrpbtcPrice\|btceurPrice\|baseAsset' src/api/client.js` -- should return zero results.
  </verify>
  <done>
Frontend symbol registry has 3 EUR-only pairs. getSymbolsForBaseAsset() and hasMultipleSymbols() deleted. API client has no cross-pair parameters (base_asset, xrpbtcPrice, btceurPrice).
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Cross-Pair UI Elements from Components and CSS</name>
  <files>
    frontend/src/components/PairingPanel.jsx
    frontend/src/components/PairingExistingTab.jsx
    frontend/src/components/SimulationModal.jsx
    frontend/src/components/PairingPanel.css
    frontend/src/hooks/useLotsData.js
    frontend/src/components/Overview.jsx
    frontend/src/components/Dashboard.jsx
    frontend/src/components/Orderblock.jsx
    frontend/src/components/OrderblockZoneTable.jsx
    frontend/src/components/SymbolLayout.jsx
  </files>
  <action>
**PairingPanel.jsx:**
- Remove `crossPairMode` state variable and its setter.
- Remove `hasMultipleSymbols` check (import was from symbolRegistry).
- Remove `effectiveCrossPair` derived state.
- Remove the cross-pair toggle checkbox UI block (the checkbox that enables cross-pair pairing mode).
- Remove `getSymbolsForBaseAsset` import.
- Simplify suggestion API calls: remove `baseAsset` parameter.
- Simplify create API calls: remove `baseAsset` parameter.
- Remove any `base_asset` or `crossPair` references from state/props/callbacks.

**PairingExistingTab.jsx:**
- Remove routing_decision display (routing-badge showing "XRPEUR-Route" or "XRPBTC-Route").
- Remove routing-savings display.
- Remove cross-pair labels on pairing cards.
- Remove pair-origin badges (the blue/amber badges showing "XRPEUR"/"XRPBTC" on lot chips).
- Remove xrpbtcPrice/btceurPrice passing to simulate API call.
- Remove any `routing_decision`, `base_asset`, or cross-pair conditional rendering.

**SimulationModal.jsx:**
- Remove the dual-route comparison section if it exists (comparing XRPEUR-direct vs XRPBTC-indirect routes).
- Remove routing_decision display.
- Remove any conditional rendering based on `is_cross_pair` or `routing_decision`.
- Keep the rest of the simulation display intact (market price, BTC qty, proceeds, costs, fees, P&L, affected lots, planned order).

**PairingPanel.css:**
- DELETE these CSS rule blocks entirely:
  - `.routing-badge` and related selectors
  - `.routing-savings`
  - `.cross-pair-toggle` and `input[type="checkbox"]` nested within it
  - `.cross-pair-toggle:has(input:checked)`
  - `.pair-origin-badge`, `.pair-origin-badge.pair-eur`, `.pair-origin-badge.pair-btc`
  - `.cross-pair-label`
- Keep all other pairing styles intact.

**useLotsData.js:**
- Remove "Cross-Pair-Kontamination" comments (cosmetic cleanup only, no functional change).

**Overview.jsx:**
- Remove cross-pair comments (cosmetic cleanup only).

**Dashboard.jsx:**
- Remove cross-pair comment (cosmetic cleanup only).

**Orderblock.jsx, OrderblockZoneTable.jsx, SymbolLayout.jsx:**
- Check for any XRPBTC or cross-pair references. Remove comments or dead code if found. These files likely have no cross-pair logic but may have cosmetic comments from earlier phases.
  </action>
  <verify>
Run `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -r 'XRPBTC\|crossPair\|cross-pair\|cross_pair\|routing_decision\|routing-badge\|pair-origin-badge\|getSymbolsForBaseAsset\|hasMultipleSymbols\|xrpbtcPrice\|btceurPrice\|base_asset\|baseAsset' src/ --include='*.jsx' --include='*.js' --include='*.css' | grep -v node_modules` -- should return zero results.
Run `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npm run build` -- build must succeed without errors.
  </verify>
  <done>
No cross-pair UI elements remain in any frontend component. No XRPBTC references in JSX, JS, or CSS files. PairingPanel has no cross-pair toggle. PairingExistingTab has no routing badges or pair-origin badges. SimulationModal has no dual-route comparison. CSS cross-pair styles deleted. Frontend builds successfully.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -rn 'XRPBTC' src/` -- zero results
2. `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -rn 'cross.pair\|crossPair\|cross_pair' src/` -- zero results (case-insensitive check recommended)
3. `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -rn 'routing_decision\|routing-badge\|routing-savings' src/` -- zero results
4. `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && grep -rn 'pair-origin-badge\|pair-origin' src/` -- zero results
5. `cd /Users/ikoerber/AIProjects/cashmgnt/frontend && npm run build` -- clean build, no errors
6. Symbol registry exports only 3 pairs: `node -e "const s = require('./src/utils/symbolRegistry.js'); console.log(Object.keys(s.KNOWN_PAIRS))"` or equivalent ESM check
</verification>

<success_criteria>
- Zero XRPBTC references in frontend src/
- Zero cross-pair UI elements (toggle, badges, routing display, dual-route comparison)
- Symbol registry has exactly 3 EUR-only pairs
- API client sends no cross-pair parameters
- Frontend builds without errors
- PairingPanel.css has no cross-pair style rules
</success_criteria>

<output>
After completion, create `.planning/phases/09-xrpbtc-removal/09-02-SUMMARY.md`
</output>
