---
phase: 09-xrpbtc-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/symbol_registry.py
  - backend/app/domain/models.py
  - backend/app/domain/pairing.py
  - backend/app/domain/lots.py
  - backend/app/domain/orders.py
  - backend/app/domain/portfolio.py
  - backend/app/services/order_service.py
  - backend/app/services/pairing_service.py
  - backend/app/services/lot_service.py
  - backend/app/services/sync_service.py
  - backend/app/services/websocket_fill_handler.py
  - backend/app/services/binance_public_client.py
  - backend/app/api/routes/pairing.py
  - backend/app/db/models.py
autonomous: true
requirements: [REM-01, REM-02, REM-03, REM-04]

must_haves:
  truths:
    - "Symbol Registry returns exactly 3 symbols (BTCEUR, ETHEUR, XRPEUR) -- no XRPBTC"
    - "get_symbols_for_base_asset() does not exist in backend code"
    - "RouteDetails, DualRouteComparison, RoutingDecision dataclasses do not exist"
    - "Pairing creation works for EUR-quoted pairs without base_asset, routing_decision, or cross-pair logic"
    - "Order creation works without route selection block or routing_decision persistence"
    - "Sync service does not fetch quote_to_eur_rate for non-EUR lots"
    - "WebSocket fill handler gracefully skips unknown symbols (no crash on XRPBTC fill)"
    - "Satoshi encoding for sub-1 prices is retained (XRPEUR needs it)"
  artifacts:
    - path: "backend/app/symbol_registry.py"
      provides: "3-symbol EUR-only registry without get_symbols_for_base_asset()"
      contains: "BTCEUR.*ETHEUR.*XRPEUR"
    - path: "backend/app/domain/models.py"
      provides: "Simplified Pairing/PairingItem/TradeLot without cross-pair fields"
    - path: "backend/app/domain/pairing.py"
      provides: "suggest_pairings() without use_eur_cost parameter or cross-pair logic"
    - path: "backend/app/services/order_service.py"
      provides: "Direct EUR-pair order creation without route selection"
    - path: "backend/app/db/models.py"
      provides: "ORM without routing_decision_json, base_asset, quote_to_eur_rate, pairing_items.cost_eur, pairing_items.lot_symbol, sell_allocations.realized_pnl_eur columns"
  key_links:
    - from: "backend/app/services/pairing_service.py"
      to: "backend/app/domain/pairing.py"
      via: "suggest_pairings() call without base_asset/use_eur_cost"
      pattern: "suggest_pairings\\(.*market_price.*threshold"
    - from: "backend/app/services/order_service.py"
      to: "backend/app/domain/orders.py"
      via: "compute_pairing_order_params() without btceur_rate"
      pattern: "compute_pairing_order_params"
    - from: "backend/app/services/websocket_fill_handler.py"
      to: "backend/app/symbol_registry.py"
      via: "is_known_symbol() guard for unknown symbols"
      pattern: "is_known_symbol|KNOWN_PAIRS"
---

<objective>
Remove all XRPBTC cross-pair code paths from the entire backend: Symbol Registry, Domain Models, Domain Logic, Services, API Routes, and ORM definitions.

Purpose: Simplify codebase to EUR-only pairs. Remove ~300+ lines of routing, cross-pair, and dual-route code. This is the foundation for Plan 02 (Frontend) and Plan 03 (Migration + Tests).
Output: Backend that only knows BTCEUR, ETHEUR, XRPEUR. All cross-pair abstractions gone. ORM columns removed (Alembic migration in Plan 03).
</objective>

<execution_context>
@/Users/ikoerber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ikoerber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-xrpbtc-removal/09-CONTEXT.md
@.planning/phases/09-xrpbtc-removal/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove XRPBTC from Symbol Registry + Simplify Domain Models and Logic</name>
  <files>
    backend/app/symbol_registry.py
    backend/app/domain/models.py
    backend/app/domain/pairing.py
    backend/app/domain/lots.py
    backend/app/domain/orders.py
    backend/app/domain/portfolio.py
  </files>
  <action>
**symbol_registry.py:**
- Remove XRPBTC entry from KNOWN_PAIRS dict. Only BTCEUR, ETHEUR, XRPEUR remain.
- Remove `get_symbols_for_base_asset()` function entirely (1:1 mapping now, no multi-symbol per base).
- Keep all other functions (parse_symbol, get_base_asset, get_quote_asset, is_known_symbol, get_pair_label, get_base_decimals, get_quote_decimals).

**domain/models.py:**
- DELETE the `RouteDetails` dataclass entirely.
- DELETE the `DualRouteComparison` dataclass entirely.
- DELETE the `RoutingDecision` dataclass entirely.
- From `Pairing` dataclass: Remove `base_asset` field. Remove `is_cross_pair` property. Remove `net_cost_eur()` method.
- From `PairingItem` dataclass: Remove `cost_eur` field. Remove `lot_symbol` field.
- From `TradeLot` dataclass: Remove `quote_to_eur_rate` field. Keep `cost_eur` (still used for EUR lots).
- Update imports if needed (remove unused imports of Decimal, Optional from removed code).

**domain/pairing.py:**
- Remove `compute_dual_route_comparison()` function entirely.
- From `suggest_pairings()`: Remove `use_eur_cost` parameter. Remove all cross-pair base_asset logic. Remove `_lot_cost()` and `_lot_pnl_pct()` helpers if they only exist for EUR-cost mode switching (if they do useful work for EUR-only mode, simplify them instead). All lots use native quote cost (which IS EUR for all remaining pairs).
- Remove any imports of RouteDetails, DualRouteComparison from domain.models.

**domain/lots.py:**
- DELETE `compute_cross_pair_realized_pnl_eur()` function entirely.
- From `create_trade_lot_from_buy_fill()`: Remove `quote_to_eur_rate` parameter. Simplify cost_eur computation: for all remaining EUR-quoted pairs, cost_eur always equals cost_quote. Set cost_eur = cost_quote unconditionally (no need for rate-based conversion). Remove quote_to_eur_rate from TradeLot constructor.
- Remove any unused imports.

**domain/orders.py:**
- KEEP the satoshi encoding logic for sub-1 prices (XRPEUR prices are ~0.50 EUR, needs it). Per research critical finding.
- From `compute_pairing_order_params()`: Remove `btceur_rate` parameter. Remove EUR-equivalent max-value check that used btceur_rate for non-EUR pairs (all pairs are now EUR, so price IS in EUR). Simplify max-value check to use price directly.
- Update any XRPBTC-specific comments to be generic (e.g., "sub-1 prices" instead of "XRPBTC satoshi encoding").

**domain/portfolio.py:**
- Remove any "Cross-Pair-Isolation" or XRPBTC-specific comments (cosmetic cleanup).
- The actual filtering logic (events filtered by symbol) already works correctly for EUR-only pairs -- no functional change needed.
  </action>
  <verify>
Run `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.symbol_registry import KNOWN_PAIRS; print(list(KNOWN_PAIRS.keys()))"` -- must show exactly ['BTCEUR', 'ETHEUR', 'XRPEUR'].
Run `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.domain.models import Pairing, PairingItem, TradeLot; print('OK')"` -- must succeed without RouteDetails etc.
Run `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.domain.pairing import suggest_pairings; import inspect; sig = inspect.signature(suggest_pairings); assert 'use_eur_cost' not in sig.parameters; print('OK')"`.
  </verify>
  <done>
Symbol registry has 3 EUR-only pairs. RouteDetails, DualRouteComparison, RoutingDecision deleted. Pairing/PairingItem/TradeLot simplified. suggest_pairings() has no cross-pair parameters. create_trade_lot_from_buy_fill() has no quote_to_eur_rate. Satoshi encoding retained for sub-1 EUR prices.
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify Services, API Routes, and ORM Definitions</name>
  <files>
    backend/app/services/order_service.py
    backend/app/services/pairing_service.py
    backend/app/services/lot_service.py
    backend/app/services/sync_service.py
    backend/app/services/websocket_fill_handler.py
    backend/app/services/binance_public_client.py
    backend/app/api/routes/pairing.py
    backend/app/db/models.py
  </files>
  <action>
**services/order_service.py:**
- Remove the entire cross-pair route selection block (research says lines ~331-374). Orders go directly to the single EUR pair.
- Remove routing_decision persistence (routing_decision_json on PairingDB).
- Remove RoutingDecision import.
- Remove satoshi encoding import if it was imported from somewhere specific to XRPBTC (keep the general sub-1 logic in domain/orders.py).
- Remove btceur_rate / xrpbtc_price parameters from execute_pairing_orders() or equivalent functions.
- Simplify: pairing execution creates orders directly on the lot's symbol (always EUR-quoted).

**services/pairing_service.py:**
- Remove `base_asset` parameter from all functions (create_pairing, list_pairings, simulate_pairing, etc.).
- Remove dual-route comparison logic from simulate_pairing().
- Remove xrpbtc_price/btceur_price parameters from simulate.
- Remove cross-pair lot loading (loading lots from multiple symbols for one base asset).
- Simplify `_pairing_to_dict()` -- remove routing_decision_json, base_asset, cross-pair fields from serialization.
- Remove PairingItem.cost_eur and PairingItem.lot_symbol from serialization/persistence.
- Remove imports of DualRouteComparison, RouteDetails, get_symbols_for_base_asset.

**services/lot_service.py:**
- Remove `quote_to_eur_rate` parameter from `create_lot_from_buy_fill()`.
- Remove cross-pair P&L override block in sell allocation (the block that calls compute_cross_pair_realized_pnl_eur).
- Remove import of compute_cross_pair_realized_pnl_eur.
- Simplify: sell allocation always uses native EUR P&L.

**services/sync_service.py:**
- Remove `_get_quote_to_eur_rates()` method entirely.
- Remove quote_to_eur_rate passing to `create_lot_from_buy_fill()`.
- The sync is already per-symbol (called with specific symbol from frontend), so no XRPBTC syncs will occur through normal flow.

**services/websocket_fill_handler.py:**
- Remove quote_to_eur_rate fetching block (research says lines ~144-160).
- Add `is_known_symbol()` guard at the top of fill processing. If symbol is not in KNOWN_PAIRS (e.g., XRPBTC fill arrives on user stream), log info message and skip: `logger.info("Skipping fill for unknown/legacy symbol: %s", symbol)` and return `{"action": "skipped", "reason": f"unknown symbol {symbol}"}`.
- Import `is_known_symbol` from symbol_registry.

**services/binance_public_client.py:**
- Check if there are any XRPBTC-specific methods or parameters. If `get_xrpbtc_price()` or similar exists, remove. If the client is generic (just fetches prices by symbol), no changes needed. Clean up any XRPBTC comments.

**api/routes/pairing.py:**
- Remove `base_asset` query/body parameter from all endpoints (suggestions, create, list, simulate, lock, execute, delete).
- Remove xrpbtc_price/btceur_price parameters from simulate endpoint.
- Remove `get_symbols_for_base_asset` import and validation.
- Simplify: all pairing endpoints work with single symbol from URL path.

**db/models.py (ORM):**
- Remove `routing_decision_json` column from PairingDB.
- Remove `base_asset` column from PairingDB.
- Remove `quote_to_eur_rate` column from TradeLotDB. KEEP `cost_eur` on TradeLotDB.
- Remove `cost_eur` column from PairingItemDB.
- Remove `lot_symbol` column from PairingItemDB.
- Remove `realized_pnl_eur` column from SellAllocationDB.
- NOTE: These are ORM-only changes. The actual DB columns still exist until Alembic migration runs (Plan 03). SQLAlchemy will not query removed columns, which is correct. The migration in Plan 03 will physically drop them.
  </action>
  <verify>
Run `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.services.pairing_service import create_pairing; import inspect; sig = inspect.signature(create_pairing); assert 'base_asset' not in sig.parameters; print('OK')"`.
Run `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.db.models import PairingDB; cols = [c.name for c in PairingDB.__table__.columns]; assert 'routing_decision_json' not in cols; assert 'base_asset' not in cols; print('OK')"`.
Run `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.db.models import TradeLotDB; cols = [c.name for c in TradeLotDB.__table__.columns]; assert 'quote_to_eur_rate' not in cols; assert 'cost_eur' in cols; print('OK')"` -- cost_eur must still exist.
Run `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.services.websocket_fill_handler import *; print('OK')"` -- imports succeed.
  </verify>
  <done>
All services simplified: no cross-pair routing, no base_asset parameters, no dual-route comparison, no quote_to_eur_rate fetching. API routes accept only symbol (no base_asset). ORM columns for cross-pair removed (routing_decision_json, base_asset, quote_to_eur_rate, pairing_items.cost_eur, pairing_items.lot_symbol, sell_allocations.realized_pnl_eur). cost_eur on TradeLotDB retained. WebSocket fill handler has is_known_symbol() guard.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "from app.symbol_registry import KNOWN_PAIRS; assert 'XRPBTC' not in KNOWN_PAIRS; assert len(KNOWN_PAIRS) == 3; print('Registry OK')"` -- only 3 EUR pairs
2. `cd /Users/ikoerber/AIProjects/cashmgnt/backend && python -c "import app.domain.models as m; assert not hasattr(m, 'RouteDetails'); assert not hasattr(m, 'DualRouteComparison'); assert not hasattr(m, 'RoutingDecision'); print('Models OK')"` -- cross-pair models gone
3. `grep -r 'XRPBTC' backend/app/ --include='*.py' | grep -v '__pycache__' | grep -v '.pyc' | grep -v 'backfill_cost_eur'` -- should return zero results (except backfill script which is kept as reference)
4. `grep -r 'get_symbols_for_base_asset' backend/app/ --include='*.py' | grep -v '__pycache__'` -- should return zero results
5. `grep -r 'DualRouteComparison\|RouteDetails\|RoutingDecision' backend/app/ --include='*.py' | grep -v '__pycache__'` -- should return zero results
6. Backend server starts: `cd /Users/ikoerber/AIProjects/cashmgnt/backend && timeout 5 python -c "from app.main import app; print('App imports OK')"` -- no import errors
</verification>

<success_criteria>
- KNOWN_PAIRS has exactly 3 entries: BTCEUR, ETHEUR, XRPEUR
- Zero references to XRPBTC in backend/app/ (excluding scripts/backfill_cost_eur.py)
- Zero references to RouteDetails, DualRouteComparison, RoutingDecision, get_symbols_for_base_asset
- Pairing/Order/Sync services work without cross-pair parameters
- ORM definitions match simplified schema (columns removed from Python, DB migration in Plan 03)
- WebSocket fill handler skips unknown symbols gracefully
- Satoshi encoding retained for sub-1 EUR prices
</success_criteria>

<output>
After completion, create `.planning/phases/09-xrpbtc-removal/09-01-SUMMARY.md`
</output>
